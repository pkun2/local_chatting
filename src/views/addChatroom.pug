doctype html
html
  head
    title 채팅방 생성

  body
    h1 채팅방 생성

    form(action='/addchatroom', method='post')
      .form-group
        label(for='chatroomName') 채팅방 이름:
        input#chatroomName(type='text', name='name', required=true)

      button(type='submit') 생성

    // 필요에 따라 추가 스크립트 삽입
    script.
      // 여기에 JavaScript 코드를 삽입할 수 있습니다.
